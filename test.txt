#import "basic.txt"

extern 
{
	add_module(string, string) : number
	compile_module(number) : void
	run_module(number) : void
}

var hello : number = add_module("hello", "write format_string(\"hello %s\", module_args)")

compile_module(hello)
module_args = ["world"]
run_module(hello)